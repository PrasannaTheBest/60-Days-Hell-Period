// Topic: Strings
// Problem: Length of String
// Status: Pseudo Approach
// Description: Computes the length of a string without using built-in functions.

/*
PSEUDO CODE APPROACH:
Function getStringLength(str):
    length = 0
    while str[length] != null_terminator:
        length = length + 1
    return length
*/
